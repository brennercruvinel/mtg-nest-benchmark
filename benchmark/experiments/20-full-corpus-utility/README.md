# 20 full corpus utility

2026-09-14. the first file with the models that read the printed name on all 38,627 cards, and the first hit@k where every card is a query. everything before this was 100 to 1000 queries on a 512 to 1500-card gallery.

hypothesis: the text-to-image numbers of experiment 15 hold on the whole corpus: siglip2 and wemm-2b find the card by name at about 0.9 hit@1 with the full 38,627-card gallery in the way, jina at about 0.65, clip at about 0.23; and with 38,627 queries the interval shrinks enough to rank the two leaders.
method: the release stills-5models (av1 all-intra crf35 tune still, the stills media, with potion, clip, siglip2, jina-v5-omni-nano@256 and wemm-2b@256 inside), benchmark/tools/bench_full_corpus.py per preset: one query per card, "artwork of the card {name}" through the model's text tower, searched in that model's int8 image space in the file, a hit when the card's own chunk_id is in the top k; hit@1, 5, 10 with the binomial standard error and a 1000-draw bootstrap interval; then, for siglip2 on a 2000-card slice, each top-1 miss classified by the label it landed on, and hit@1 recomputed with the "name // name" rows filtered out of the gallery, for siglip2 and wemm.
verdict: refuted on the level, confirmed on the order. siglip2 0.750 and wemm-2b 0.744 hit@1 (intervals 0.745 to 0.755 and 0.740 to 0.748), jina 0.336, clip 0.098; hit@10 is 0.888, 0.913, 0.582, 0.193. the 512-card numbers were 0.93 and 0.91. most of the drop has one cause: 2,246 rows of the corpus, 5.8%, are art-series cards, full art with no frame and no printed name, labeled "name // name" in the source; they take 61% of siglip2's top-1 misses while being 8% of the gallery, and when the query card is one of them siglip2 finds it 23% of the time. with those rows out of the gallery, plain-name queries go from 0.787 to 0.881 (siglip2) and 0.802 to 0.869 (wemm) on the same slice. the rest of the gap is what a gallery seventy times larger costs: names that share a word, reprints with a second face, tokens named "Elemental" and "Spirit".

i had 0.93 in my head as the number for this corpus and it was a number for 512 cards. the honest one is 0.75, and the honest one comes with a reason i had not looked for. the art series is a scryfall product line, a card-sized print of the art with nothing else on it, and the spellbook cache has one image per oracle id, so those prints are in the corpus as cards. a model that finds the card by reading its name has nothing to read on them, and their images, borderless paintings with no chrome, seem to sit where a lot of text queries land: an unrelated query lands on an art-series row six times more often than chance. that is a hub, in the nearest-neighbour sense, and it is a property of the corpus and the space together, not of the codec.

wemm wins at hit@10 and siglip2 at hit@1, by a little in both cases, and the intervals do not overlap at hit@10. i would read that as: siglip2 is sharper on the exact name, wemm ranks the near misses better. jina at 256 dims is a different tier, and clip is what it was on every file before, a model that encodes the painting and not the words, at 0.10 with 38 thousand paintings to choose from.

the cost column is the other half of the finding. wemm-2b embedded the corpus at 1.02 images per second, 21 hours; jina at 3.5, six hours; siglip2 at 53, twelve minutes, and siglip2 is the leader at hit@1. for this corpus and this query the model that reads the name best is also the one that costs a hundredth of the build time of the one that ties it. the text side is the same story: siglip2 embeds 81 queries a second, wemm 15.

what this does not settle: the gate still measures with clip; the utility floor of nest #135 should run with siglip2 on this file, and that is a separate experiment. and the art-series rows are a decision, not a bug: they are cards in scryfall's sense, and the corpus keeps them, but a retrieval benchmark on "find the card by its name" should report them apart, which this one now does.

provenance: release/v0.3/stills-5models, file_hash 6b2bc21a, built 2026-09-12 to 14 on apple m4 (nest forge at cbc326de, the worktree the build started in). bench on 2026-09-14, mps fp16, one preset per process. raw json under `data/`: the four full-corpus runs, the miss classification, the double-face count and the filtered-gallery run.
